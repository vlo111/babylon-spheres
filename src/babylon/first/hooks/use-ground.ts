export const useGround = () => {
    const width = 24;
    const height = 24;
    const subdivisions = 2;
    const receiveShadows = true;

    return { width, height, subdivisions, receiveShadows };
};
